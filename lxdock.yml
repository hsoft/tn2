name: tn2
image: debian/stretch
hostnames:
  - tn2.local

users:
  - name: tn2

shell:
  user: tn2
  home: /opt/tn2

shares:
  - source: .
    dest: /nomadshare
    set_host_acl: false

provisioning:
  - type: ansible
    playbook: ansible/site.yml
